# ─────────────────────────────────────────────────────────────
# 都是可选的。一个都不填也能跑 —— fork 下来什么都不改即可。
# 构建时读取：`VITE_XXX=... npm run build:site`，或复制成 .env 放在仓库根。
# ─────────────────────────────────────────────────────────────

# Google 单点登录的客户端 ID。不填也能跑 —— 登录页会退回账密登录。
# 到 https://console.cloud.google.com/apis/credentials 建「Web 应用」类型的
# OAuth 2.0 客户端 ID，并把站点源加进「已获授权的 JavaScript 来源」：
#   https://decli.github.io
#   http://127.0.0.1:5173
VITE_GOOGLE_CLIENT_ID=

# Google Analytics 4 的衡量 ID，形如 G-XXXXXXXXXX。
# 到 https://analytics.google.com → 管理 → 数据流 → 新建「网站」数据流，
# 网址填 https://decli.github.io/ftms/，建完右上角就是这个 ID。
#
# 不填的后果是**一行统计脚本都不会加载**（不是加载了不发）。所以：
#   · 本地开发不用填，不会往线上打脏数据
#   · fork 这个仓库的人不填，就不会替别人收数据
# 另外浏览器开了「请勿跟踪」时也会自动关闭，见 src/lib/analytics.ts。
VITE_GA_ID=

# 站点的正式地址，决定 canonical / og:url / sitemap 里写什么。
# 不填就用 vite.config.ts 里的默认值 https://decli.github.io + /ftms/。
# 换自己的域名时改这两个（末尾的斜杠要带上）。
VITE_SITE_ORIGIN=
VITE_SITE_PATH=
